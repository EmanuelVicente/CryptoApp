import { StyleSheet } from "react-native";

export default StyleSheet.create({
	input: {
		backgroundColor: "#ffffff",
		borderWidth: 1,
		height: 32
	},
	textErrorButton: {
		bottom: -20,
		color: "#ff0000",
		fontSize: 12,
		left: 10,
		position: "absolute"
	}
});
